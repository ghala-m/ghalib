# تحديثات غالِب — الدفعة الخامسة والعشرون

## طريقة التطبيق
نسخة كاملة (zip)، مو باتش. **شغّلي المايجريشن كامل الآن** بـSQL editor (نفس النص موجود بالمحادثة):
```sql
alter table public.course_items add column if not exists from_syllabus boolean not null default false;
alter table public.courses add column if not exists calendar_default_view text not null default 'week';
alter table public.courses drop constraint if exists courses_calendar_default_view_check;
alter table public.courses add constraint courses_calendar_default_view_check
  check (calendar_default_view in ('day', 'week', 'month'));
```

## إصلاح اختفاء توزيع الدرجات (بسبب مني — ما عطيتك المايجريشن واضح بالرد السابق)
لأن العمود الجديد مو موجود على قاعدة بياناتك، عملية رفع السيلابس كانت تمسح توزيع الدرجات القديم بنجاح، ثم تتوقف بخطوة تالية تعتمد على عمود غير موجود — فتضل الدرجات فاضية دايمًا. أعدت ترتيب الكود بـ`SyllabusPanel.tsx`: الحين توزيع الدرجات يُحذف *ويُعاد إدراجه فورًا* كخطوة واحدة متكاملة، قبل أي خطوة تعتمد على العمود الجديد — فحتى لو صار خطأ بخطوة تالية، الدرجات تبقى سليمة.

## لوحة التحكم: خلفية أبرز شوي
أضفت طبقة نقش نقطي خفيف (يشبه ورق الدفاتر) فوق التدرج اللوني الموجود بكل الصفحات، ولوحة التحكم تحديدًا صارت أقوى شوي (تدرجات أوسع + لمسة لون إضافية) عشان تحس إنها "الصفحة الرئيسية" مقارنة بالصفحات الثانوية. القوة الحالية تقديرية — قوليلي رأيك بعد النشر وأعدّلها أقوى أو أخف.


## طريقة التطبيق
نسخة كاملة (zip)، مو باتش. **فيها migration جديد**: `20260922010000_syllabus_reset_flag.sql` — شغّليه بـSQL editor.

```sql
alter table public.course_items add column if not exists from_syllabus boolean not null default false;
alter table public.courses add column if not exists calendar_default_view text not null default 'week';
alter table public.courses drop constraint if exists courses_calendar_default_view_check;
alter table public.courses add constraint courses_calendar_default_view_check
  check (calendar_default_view in ('day', 'week', 'month'));
```

## إصلاح تكرار عناصر السيلابس عند إعادة الرفع (السبب الجذري لصورتك)
كل تصنيف بتوزيع الدرجات كان يتضاعف لأن رفع السيلابس كان يُدرج فوق البيانات القديمة بدل ما يستبدلها. الحين: أوزان الدرجات (`grade_weights`) تُمسح بالكامل قبل كل رفع جديد (دايمًا مصدرها السيلابس وحده، آمن نمسحها). عناصر المادة (`course_items`) تُمسح بس اللي مصدرها سيلابس سابق (عمود جديد `from_syllabus`) — أي مهمة ضفتيها يدويًا بنفسك ما تنمسح.

## تقويم أسبوعي افتراضي لكل مادة، وقابل للتغيير
تقويم صفحة المادة صار افتراضيًا أسبوعي (كان شهري). أضفت اختيار صغير بجانب عنوان التقويم يحفظ التفضيل (يومي/أسبوعي/شهري) لهذي المادة تحديدًا بقاعدة البيانات (عمود جديد `calendar_default_view`).

## قسم "ملفات هذي المادة" بصفحة المادة
أسفل شبكة السيلابس وتوزيع الدرجات مباشرة — يسحب من مكتبة مواد الدراسة (الميزة من الدفعة الماضية) ويعرض كل ملخص/بطاقات/اختبار مرتبط بهذي المادة تحديدًا، مع رابط "مشاهدة الكل" يوديك لمكتبة المواد الكاملة.

## مكتبة المواد: خيار عرض (قائمة / شبكة ٣ أعمدة)
زرين صغار بأعلى الصفحة يبدّلون بين عرض قائمة وعرض شبكي، محفوظ محليًا بمتصفحك. البطاقة المفتوحة بوضع الشبكة تتوسّع عبر الأعمدة الثلاثة بدل ما تنكسر.

## حذفت رابط "مكتبة المواد" من القائمة الجانبية
بناءً على طلبك — الوصول لها الحين بس عبر زر "مشاهدة الكل" (من صفحة المادة أو من أدوات الدراسة)، مو من السايد بار.

## ما لم أنجزه بعد من طلبك — بصراحة
هذي الأجزاء أكبر وأعقد من إني أخاطر أسويها بسرعة بنفس الرد، فأجّلتها:
- نقل زر رفع السيلابس لقسم "تعديل" + زر "إعادة التعيين" و"ترتيب" لترتيب أقسام الصفحة يدويًا (نظام سحب-وإفلات محفوظ لكل مادة)
- استبدال بطاقة "الخطة الدراسية" بخطة أسبوعية مولّدة بالذكاء الاصطناعي (تذكيرات، نصائح مذاكرة) بدل زر رفع السيلابس

قوليلي إذا تبين أبدأ فيهم بالرد الجاي، وأي وحدة منهم أولوية أكبر عندك.


## طريقة التطبيق
نسخة كاملة (zip)، مو باتش. فكي الضغط وارفعيها لـ GitHub (لاحظي: حذفت `bun.lock` القديم عمدًا — لوفابل بيولّد نسخة جديدة تلقائيًا أول نشر).

## إصلاح فشل النشر بالكامل (bun install)
السبب: لوفابل يستخدم Bun للتثبيت، وBun يرفض تلقائيًا أي إصدار حزمة نُشر خلال آخر ٢٤ ساعة (حماية أمنية ضد هجمات سلسلة التوريد). حزم `@ai-sdk/openai` و`@ai-sdk/google` و`@ai-sdk/anthropic` اللي أضفتها بالدفعة السابقة كانت أحدث إصدار متاح وقتها، فصادف نشرها قبل أقل من يوم. ثبّتها الآن بإصدارات محددة بدقة (بدون `^`) أقدم بعدة أيام، وحذفت `bun.lock` عشان يتولّد من جديد مطابق تمامًا.

## نموذج تعديل/إضافة مادة — ثلاث مشاكل حقيقية صلّحتها
- **الفصل صار قائمة منسدلة من فصولك الفعلية** (مو نص حر). هذا يحل مشكلة أعمق كانت موجودة من قبل بالمحادثة: أي خطأ إملائي بسيط بكتابة اسم الفصل يدويًا كان يكسر مطابقة المعدل الفصلي بمخطط "تطوّر المعدل" لأنه يعتمد مطابقة نصية حرفية.
- **حقل "الدرجة النهائية" ما يظهر إلا للمواد المكتملة فقط** — منطقي، لأن مادة "أدرسها حاليًا" أو "مستقبلية" ما عندها درجة نهائية بعد. وإذا كانت مادة عندها درجة محفوظة من قبل وغيّرتِ حالتها لغير مكتملة، ما تُحفظ الدرجة القديمة معاها.
- **الساعات صارت اختيار "مادة (٣) / لاب (١) / مخصص"** بدل رقم فاضي — يملأ تلقائيًا، مع خيار "مخصص" لو المادة عندها ساعات مختلفة فعلاً.

---



## طريقة التطبيق
نسخة كاملة (zip)، مو باتش. فكي الضغط وارفعيها لـ GitHub. **فيها migration جديد** (`20260919120000_study_materials.sql`) — شغّليه بـSQL editor زي باقي المايجريشن السابقة.

## إصلاح: قائمة "المادة المرتبطة" بإضافة حدث
كانت تعرض كل المواد (كل الفصول، حتى المكتملة والمستقبلية) بدون فلترة. صارت تعرض بس مواد الفصل الحالي (`status === "current"`)، وبالشكل المطلوب: `كود المادة · اللقب` — `src/components/app/EventDialog.tsx`.

## فصل الذكاء الاصطناعي عن رصيد لوفابل (BYOK)
أعدت بناء `src/lib/ai-gateway.server.ts` بدالة `getAiModel()` جديدة تفحص بالترتيب: `OPENAI_API_KEY` → `GOOGLE_GENERATIVE_AI_API_KEY` (فيه مستوى مجاني حقيقي، أنسب خيار) → `ANTHROPIC_API_KEY` → أخيرًا بوابة لوفابل كما هي (الوضع الافتراضي بدون أي إعداد إضافي). أول سر موجود من الثلاثة يفوز. طبّقتها على كل ملفات `.functions.ts` الستة (advisor, tools, syllabus, academic-calendar, majorsheet, transcript-import) بدل التكرار اليدوي لقراءة `LOVABLE_API_KEY`.

## مكتبة مواد الدراسة (جديد بالكامل)
- جدول `study_materials` (مايجريشن جديد) يخزن أي ملخص/بطاقات/اختبار سوّيتيه بأدوات الدراسة، مربوط اختياريًا بمادة.
- صفحة أدوات الدراسة صار فيها اختيار "المادة المرتبطة" (مواد الفصل الحالي بس)، وأي نتيجة تتولد تنحفظ تلقائيًا بالمكتبة.
- بعد توليد بطاقات مراجعة تحديدًا، توجّهك تلقائيًا لصفحة المكتبة الجديدة وتفتحها مباشرة — بالضبط زي ما طلبتِ.
- صفحة جديدة `/study-library`: كل مادة تظهر كقسم فيه كل الملخصات/البطاقات/الاختبارات المحفوظة لها، قابلة للفتح/الحذف. رابط جديد بالقائمة الجانبية.


## طريقة التطبيق
نسخة كاملة (zip)، مو باتش. فكي الضغط وارفعيها لـ GitHub.

## إصلاح اختفاء أسماء الشهور بملف PDF
السبب: دمج `angle: 90` مع `align: "center"` بمكتبة jsPDF يحسب نقطة الارتكاز غلط تمامًا (تأكدت منه باختبار فعلي — كان يرسم النص خارج صندوقه المخصص بعشرات النقاط)، فأعمدة الأسابيع اللي تُرسم بعده كانت تغطيه بالكامل. صححتها بحساب موضع التوسيط يدويًا (بدون `align`) بـ`src/lib/term-calendar-pdf.ts`. اختبرت الإصلاح بالسياق الكامل (٤ أشهر + أعمدة أسابيع فوقها) وتأكدت إن كل الأسماء تظهر بمكانها الصحيح بدون أي تغطية.

## حذف أزرار "طباعة" نهائيًا من الموقع
شلتها من الصفحات الثلاث (كشف الدرجات، طباعة الخطة، تقويم الفصل) — بقي بس زر "تنزيل PDF" بكل صفحة. طباعة المتصفح العادية (Ctrl+P) تشتغل عادي لمن يحتاجها يدويًا، بس الزر المخصص انحذف بالكامل زي ما طلبتي.


## طريقة التطبيق
نسخة كاملة (zip)، مو باتش. فكي الضغط وارفعيها لـ GitHub.

## تصحيح جوهري: كنت فاهم بنية التصميم غلط بالدفعة اللي قبل هذي
الصورة الأخيرة (الرسمة الاحترافية) وضّحت البنية الحقيقية اللي كانت مختلفة عن فهمي السابق:
- **الأشهر = صفوف** (مو كتل منفصلة، وليست أعمدة)
- **الأسابيع = أعمدة جنب بعض داخل صف الشهر**، أول أسبوع بالشهر يلاصق خانة اسم الشهر، والأسابيع التالية تكمل بجانبه
- **الأيام = ٧ أعمدة نحيفة وطويلة داخل كل أسبوع** (سبت→جمعة)، مو صف عريض قصير — والطول تحديدًا عشان يصير فيه مساحة كتابة فعلية تحت كل تاريخ، بالضبط زي ما وضّحتي

أعدت بناء **الاثنين معًا** بهذي البنية الصحيحة:
- `src/lib/term-calendar-pdf.ts`: صفحة واحدة (حجم A3 landscape — مو A4، لأن A4 يخلي عمود اليوم ضيق جدًا يفقد الهدف من "مساحة الكتابة"؛ هذا مستند مخصص لطباعة كبيرة أو تكبير عند الطباعة العادية)، الأشهر صفوف من فوق لتحت، عرض كل خانة أسبوع يتمدد تلقائيًا حسب عدد أسابيع شهره (شهر بأسبوعين خاناته أعرض من شهر بخمس أسابيع — بالضبط زي صورتك)
- `src/components/app/SemesterGrid.tsx` (شبكة التقويم على الشاشة): نفس البنية بالضبط، بـflexbox (كل أسبوع `flex-1` داخل صف شهره، فيحصل نفس تمدد العرض تلقائيًا بدون حساب يدوي)، مع تمرير أفقي لو الشهر عنده أسابيع كثيرة ما تناسب عرض الشاشة

## شلت الألوان المفروضة نهائيًا من كل مكان (تأكيد)
أبيض وأسود مع تظليل رمادي خفيف بس على الرؤوس، بالملف وبالشاشة.

## اختبار فعلي
ولّدت PDF فعلي بمحاكاة فصل حقيقي (سبتمبر أسبوعين، أكتوبر/نوفمبر/ديسمبر خمس أسابيع لكل وحد) وتأكدت رقميًا إن عرض خانة سبتمبر ٥٥٤pt مقابل ٢٢٢pt لخانة أكتوبر — يعني التمدد التلقائي يشتغل صح ومطابق لمنطق صورتك.


## طريقة التطبيق
نسخة كاملة (zip)، مو باتش. فكي الضغط وارفعيها لـ GitHub.

## تصحيح: كل الأشهر بمستند واحد متواصل، مو صفحة منفصلة لكل شهر
أعدت كتابة `src/lib/term-calendar-pdf.ts` بالكامل: بدل ما تبدأ صفحة جديدة إجباريًا مع كل شهر، الحين الأشهر تُرسم متتالية بنفس المستند (بالضبط زي ترتيب العرض على الشاشة بمكوّن `SemesterGrid.tsx`)، وصفحة جديدة تُفتح فقط لما الشهر التالي فعليًا ما عنده مساحة كافية بالصفحة الحالية — مو بشكل إجباري ثابت. اختبرت هذا فعليًا بمحاكاة فصل كامل (٤ أشهر): النتيجة مستند من صفحتين بس (تدفّق طبيعي حسب المساحة)، مو ٤ صفحات منفصلة.

## شلت الألوان المفروضة من ملف PDF
كانت الخلية تُلوَّن بالسماوي/الوردي/الأصفر تقريبًا بحسب ألوان رسمتك. بما إنك وضّحتي إن الألوان كانت للتوضيح بس أثناء شرح التصميم، صار التصميم أبيض وأسود نظيف (تظليل رمادي خفيف بس على رؤوس الأعمدة/الصفوف) — أنسب للطباعة الفعلية.

## نفس التصميم صار على الموقع نفسه، مو بس الملف
اسم الفصل (زي "Fall 2026/2027") صار يظهر كشريط عنوان واضح المعالم (حدود + خلفية خفيفة) بدل نص عادي بمنتصف الصفحة — بمكوّن `SemesterGrid.tsx`. باقي البنية (الأشهر كتلة تلو الأخرى، الأسبوع كصف برقم واضح، ٧ أعمدة أيام) كانت أصلاً مطابقة لترتيب رسمتك من قبل، فما احتاجت إعادة بناء — بس حسّنت وضوح شريط اسم الفصل. تلوين أيام الشهر المجاور بالأسود (من الدفعة اللي قبلها) موجود وشغّال بنفس المكوّن.

## تنبيه صادق
لفصل دراسي كامل (~١٦-١٧ أسبوع)، المستند قد يمتد لصفحتين فعليًا (زي ما تأكد بالاختبار) — مو لأن كل شهر بصفحة، بس لأن المحتوى فعليًا أطول من ارتفاع صفحة A4 وحدة بحجم خلايا مريح للقراءة والكتابة. إذا تبين إجبار كل شي بصفحة وحدة مهما كان الحجم، أقدر أصغّر ارتفاع الصف أكثر — بس بيصير أضيق لمساحة الكتابة اللي طلبتيها بالتصميم الأصلي.

---



## طريقة التطبيق
نسخة كاملة (zip)، مو باتش. فكي الضغط وارفعيها لـ GitHub.

## السبب الحقيقي وراء استمرار فشل PDF بعد إصلاح الـCDN
فحصت `styles.css`: كل نظام الألوان بالتطبيق (الخلفية، النص، accent، كل شي) معرّف بدالة `oklch()` — ٨٧ استخدام. مكتبة `html2canvas` (اللي نبني عليها تصوير الصفحة) **ما تقدر تقرأ ألوان oklch إطلاقًا**، بغض النظر عن كونها محمّلة من npm أو CDN. يعني إصلاح الـCDN بالدفعة الأولى كان صحيح وضروري، لكنه ما كان كافي — المشكلة الجذرية أعمق من مصدر تحميل المكتبة.

## الحل: حذفت أسلوب التصوير (html2canvas) نهائيًا من التطبيق
بعد كشف الدرجات، حوّلت **طباعة الخطة** و**تقويم الفصل** كمان لملفات PDF نصية حقيقية تُرسم مباشرة بـ`jsPDF` (بدون قراءة أي CSS من الصفحة إطلاقًا، فمشكلة oklch ما تقدر تصير أصلاً):

- **`src/lib/plan-pdf.ts`**: جدول لكل تصنيف مادة (تمهيدي/عام/كلية/تخصص/اختياري تخصص)، بنفس منطق `jspdf-autotable` المستخدم بكشف الدرجات.
- **`src/lib/term-calendar-pdf.ts`**: رسمته بالكامل يدويًا (مو جدول تلقائي) اعتمادًا على تصميمك المرفق بالضبط — صفحة لكل شهر (landscape)، صف لكل أسبوع (رقم الأسبوع مستمر عبر الفصل كامل، مو يتصفّر كل شهر)، وعمود لكل يوم من السبت للجمعة، بنفس لغة الألوان التقريبية من رسمتك (سماوي=الفصل، بنفسجي فاتح للعنوان، وردي=الأسبوع، أصفر=الأيام).
- حذفت `src/lib/export-pdf.ts` نهائيًا وشلت `html2canvas` من الاعتماديات — ما بقت مستخدمة بأي مكان بالكود (تبقى موجودة بس كاعتمادية غير مباشرة لمكتبة jsPDF نفسها لميزة اختيارية إحنا ما نستخدمها إطلاقًا، فما تُحمَّل لأي مستخدم فعليًا).

## أيام الشهر السابق/التالي بالأسود
بمكوّن `src/components/app/SemesterGrid.tsx` (شبكة التقويم على الشاشة) ونفس المنطق بملف PDF الجديد: رقم اليوم للأيام الطافحة من شهر مجاور صار **أسود** بدل الرمادي الباهت السابق — البيانات نفسها (علم `overflow`) كانت جاهزة بمكتبة `src/lib/semester-grid.ts` من الأساس، بس التلوين على الشاشة ما كان مطبّق صح.

## ملاحظة صادقة: تفسيري لتصميمك
اعتمدت قراءتي للتصميم المرفق كالتالي: صفحة منفصلة لكل شهر (بدل الكل بصفحة وحدة) عشان تصير خانة كل يوم فيها مساحة فعلية للكتابة بدل ما تكون مضغوطة جدًا على ورقة A4 وحدة. إذا شكل الناتج بعد التنزيل يختلف عن تصورك، وصفيلي الفرق بالضبط وأعدّله.


## طريقة التطبيق
نسخة كاملة (zip)، مو باتش. فكي الضغط وارفعيها لـ GitHub.

## كشف الدرجات: تحويله لملف PDF نصي حقيقي (jsPDF + jspdf-autotable) بدل تصوير الصفحة
بدل الاعتماد على html2canvas (يصوّر الصفحة كصورة ويحطها بملف PDF)، كشف الدرجات تحديدًا صار يُبنى كنص حقيقي مباشرة بـ`src/lib/transcript-pdf.ts` عبر `jsPDF` + `jspdf-autotable` — من أشهر وأكثر مكتبات توليد PDF استخدامًا بجافاسكربت. الفرق العملي: نص قابل للتحديد والنسخ والبحث، حجم ملف أصغر بكثير، ما يعتمد على canvas ولا على ألوان CSS الحديثة (oklch) اللي سبّبت مشاكل سابقة، ومضمون يشتغل بأي متصفح.

**بالمقابل، وهذا قرار متعمد:** خطوط jsPDF القياسية (Helvetica) ما تدعم حروف عربية إطلاقًا — أي نص عربي يتحول لمربعات فاضية أو يختفي. فالملف الناتج **بالإنجليزية دائمًا**، بغض النظر عن لغة واجهة التطبيق وقت التصدير. بما إن رموز وأسماء المواد بالتطبيق إنجليزية أصلاً بحكم العرف الجامعي، هذا ما يفقد أي معلومة عملية.

**صفحتا "طباعة الخطة" و"تقويم الفصل" بقيتا على طريقة التصوير (html2canvas) القديمة** — نفس الإصلاح اللي صار سابقًا (npm بدل CDN) لسا فعّال فيهم. السبب: محتواهم رسومي بطبيعته (خريطة مسقطات بأسهم، جدول أسبوعي بمربعات) مو جداول نصية، فإعادة بنائهم بـjsPDF نص-فقط يفقد الشكل البصري الفعلي بدل ما يحسّنه. إذا حسّيتي إن أحد هذولين يحتاج نفس المعالجة، قوليلي.

## باقي عناصر القائمة
تصحيح رسالة الخطأ، المؤثرات الصوتية والبصرية الموسّعة، تبسيط مخطط المسقطات، تنبيه فجوة مخطط تطور المعدل، و`.env` مع `VITE_VAPID_PUBLIC_KEY` — كلها كانت مُنجزة من دفعات سابقة وموجودة كاملة بهذا الملف المضغوط، ما تغيّر فيها شي جديد هالمرة.

---



## طريقة التطبيق
نسخة كاملة (zip)، مو باتش. فكي الضغط وارفعيها لـ GitHub.

## تحديث ملف .env بمفتاح VAPID العام
لوحة Secrets بلوفابل ترفض أي اسم يبدأ بـ VITE_ (لأنها قيم تُبنى وقت البناء بالمتصفح، مو أسرار خادم) وتطلب تحطينها بملف .env مباشرة. أضفت `VITE_VAPID_PUBLIC_KEY` لملف `.env` نفسه بالمشروع (نفس المكان اللي فيه إعدادات Supabase العامة أصلاً) بنفس قيمة `VAPID_PUBLIC_KEY` اللي ولّدتها لك. بعد رفع هذا الملف وعمل Publish، القيمة تكون متاحة بالمتصفح تلقائيًا.

## نسخة آمنة للتكرار من SQL المايجريشن
ظهر خطأ "push_subscriptions already exists" لما جربتِ تشغيل السكربت — يعني جزء من الجداول كان موجود مسبقًا من محاولة سابقة. أعدت كتابة كل السكربت بصيغة آمنة للتشغيل أي عدد مرات (`create table if not exists`، و`drop policy if exists` قبل كل `create policy`) — تقدرين تشغلينه من جديد بالكامل بدون قلق، أي جزء موجود بيتجاوز تلقائيًا. (نص السكربت الجديد بالرد بالمحادثة، مو ملف منفصل بالمستودع، لأنه يُشغَّل يدويًا مرة وحدة بـSQL editor مو جزء من كود التطبيق).

---



## طريقة التطبيق
نسخة كاملة (zip)، مو باتش. فكي الضغط وارفعيها لـ GitHub.

## تبسيط مخطط المسقطات — التوهج والخطوط صارت للمواد الحالية فقط
كان أي مادة بحالة "متاحة" (استوفت شروط التسجيل بس ما أُخذت بعد) تتوهج، وأي خط يؤدي لمادة "متاحة" أو "حالية" يضوي ويتحرك. بخطة دراسية كاملة، أغلب المواد المتبقية تكون "متاحة" بمرحلة أو بأخرى، فيصير المخطط كله شبه مضوي ومتحرك بنفس الوقت — وهذا بالضبط سبب الإحساس إنه "زائد ومبالغ فيه".

عدّلته يصير:
- **التوهج على العقدة** (`src/components/app/PrereqFlowChart.tsx`، `src/styles.css`): بس المواد بحالة "حالية" (current) — يعني اللي تدرسينها الفصل هذا فعلاً، مو أي مادة متاحة للتسجيل مستقبلاً.
- **الخطوط المضيئة المتحركة**: بس الخطوط الخارجة من مادة حالية إلى المادة اللي ستفتحها — إجابة مباشرة على سؤال "إذا خلصت المادة اللي آخذها الحين، وش بتفتح لي؟" بدل تمييز كل مسار ممكن بالخطة.

باقي المواد والخطوط تظل ظاهرة بنفس مكانها (رمادية هادئة بدون حركة) — ما حذفت أي معلومة، بس قلّلت أيش اللي يلفت الانتباه بنفس اللحظة.

---



## طريقة التطبيق
نسخة كاملة (zip) من المستودع، مو باتش. فكي الضغط وارفعيها لـ GitHub.

## سبب عدم ظهور الفصل الأول بمخطط "تطوّر المعدل"
مو خلل عرض — الفصل ببساطة ما عنده معدل محسوب فعليًا، فيُستبعد من الرسم البياني بصمت. السبب من `src/lib/gpa.ts`: أي فصل يُحسب معدله إما من قيمة GPA مُدخلة يدويًا بجدول `terms` (عبر "سجل تاريخ المعدل" بصفحة الملف الشخصي)، أو من متوسط الدرجات الفعلية للمواد المرتبطة بنفس اسم الفصل. إذا الفصل الأول عنده مواد بدون درجة نهائية مسجّلة (final_grade فاضي) ومافي سجل GPA يدوي له بجدول terms، تجيه قيمة null ويُستبعد.

**كيف تحلّينها:** الملف الشخصي → قسم "سجل تاريخ المعدل" (GPA History) → عدّلي/أضيفي سجل الفصل الأول يدويًا (المعدل + عدد الساعات).

**إصلاح إضافي بالكود:** التنبيه اللي المفروض يوضح إن فيه فصل ناقص كان يظهر بس لو كل البيانات تقريبًا مفقودة (`data.length < 2`). عدّلته يقارن بعدد كل الفصول الفعلي (`history.length`) بدل رقم ثابت، فالحين لو فصل واحد بس ناقص من أصل ٣ أو أكثر، يظهر تنبيه صغير فوق المخطط "١ فصل بدون معدل مسجّل" مع رابط مباشر لصفحة التعديل — بدل الاستبعاد الصامت.

---



## طريقة التطبيق
هذي الدفعة عبارة عن نسخة كاملة (zip) من المستودع بعد التعديلات، مو باتش. فكي الضغط وارفعيها لـ GitHub (أو اسحبيها فوق مجلد المشروع محليًا ثم `git add . && git commit`). لا يوجد migration جديد بهذي الدفعة.

## تصدير PDF: تحويله من CDN إلى حزمة npm حقيقية
`html2canvas` و`jsPDF` كانا يتحمّلان وقت الضغط على الزر من سكربتات خارجية (`cdnjs.cloudflare.com` / `cdn.jsdelivr.net`) بدل ما يكونان جزء من المشروع. أي حاجب إعلانات أو جدار حماية شركة/جامعة يحجب هذي النطاقات يفشّل التصدير بالكامل وبصمت — نفس الأعراض اللي وصفتها "ما يتولّد اساسًا". حوّلتهما لاعتماديتين حقيقيتين بـ`package.json` (`html2canvas`, `jspdf`) يتم استيرادهما بـ`import()` ديناميكي (نفس فكرة الكسل بالتحميل، بس من داخل حزمة المشروع نفسها مو من الإنترنت) — `src/lib/export-pdf.ts`. تأكدت إن `npm run build` يمر بدون أي خطأ بعد التعديل.

## اكتشفت سبب رسالة "تعذّر الرد من المرشد" العامة بدل رسالة "انتهى الرصيد"
هذا اكتشاف مهم غير مرتبط مباشرة برصيد لوفابل: دالة `aiError()` بملف `src/lib/ai-gateway.server.ts` كانت تحاول تكتشف نوع الخطأ (رصيد منتهي / rate limit / محظور) بالبحث عن الرقم `"402"` أو `"429"` أو `"403"` **داخل نص رسالة الخطأ**. المشكلة: مكتبة `ai` (Vercel AI SDK) ما تحط رمز حالة HTTP داخل `message` أبدًا — تحطه بخاصية منفصلة اسمها `statusCode`. يعني الفحص القديم ما كان يطابق أبدًا، حتى لو كان السبب الحقيقي فعلاً 402 (رصيد منتهي)، فيرجع دايمًا للرسالة العامة "تعذّر الرد من المرشد" بدل رسالة "نفدت أرصدة الذكاء الاصطناعي" الواضحة. صحّحتها تقرأ `error.statusCode` مباشرة عبر `APICallError.isInstance()` من نفس المكتبة. هذا ما يحل مشكلة الرصيد نفسها (لازم تُشحن من لوحة لوفابل)، لكنه يضمن إن الرسالة اللي تظهرين تكون دقيقة بعد كذا بدل ما تكون مضللة.

## مؤثرات صوتية وبصرية عند الإنجازات وتسجيل المذاكرة
- لاحظت إن فيه صوت جاهز أصلاً (`src/lib/sound.ts`, `playUnlockChime`) عند فتح إنجاز جديد بصفحة "الملف الأكاديمي"، لكن بدون أي مؤثر بصري يرافقه غير Toast عادي.
- أضفت مكوّن `src/components/app/ConfettiBurst.tsx` — كونفيتي خفيف بـCSS خالص (بدون أي مكتبة خارجية، بنفس فلسفة تفادي الاعتماد على مصادر خارجية) يظهر فوق شارات الإنجازات لحظة فتح إنجاز جديد.
- أضفت صوت أخف (`playTickChime`) وكونفيتي أصغر عند الضغط على "سجّل اليوم" بمكوّن الستريك (`StudyStreak.tsx`) — تحفيز بصري/صوتي لأكثر تفاعل يومي بالتطبيق.
- كل هذا يحترم `prefers-reduced-motion` تلقائيًا ولا يحتاج أي رصيد لوفابل (كله محلي بالمتصفح).

---



## طريقة التطبيق
فك الضغط داخل المشروع. لا يوجد migration جديد بهذي الدفعة.

## إصلاح PDF — هالمرة بطريقة مختلفة جذريًا

رسالتك الثانية (`unsupported color function "oklch"` بدل `oklab`) أكدت إن أسلوبي السابق (تصحيح كل عنصر بالصفحة واحد واحد) ناقص — دايمًا فيه احتمال (تدرج لوني، ظل، SVG) يفوتني. غيّرت الاستراتيجية بالكامل: بدل ما أطارد كل مكان يمكن يستخدم لون، أصحح **مصدر الألوان نفسه** — متغيرات التصميم (`--accent`, `--border`, `--background`... إلخ بملف `styles.css`) اللي كل شي بالتطبيق يرجع لها أصلاً (التدرجات، الظلال، رسومات SVG كلها تستخدم نفس المتغيرات). أحوّل كل متغير لصيغة RGB عادية *قبل* التقاط الصورة مباشرة، ألتقط الصورة، وأرجّعها لوضعها الأصلي فورًا. أي عنصر بالصفحة يستخدم هذي المتغيرات ينصلح تلقائيًا بدون ما أحتاج أطارده — `src/lib/export-pdf.ts`.

## توضيح: "Supabase" مو شي جديد تحتاج تسويه

لاحظت إنك ما كنت متأكد وش أقصد بـ"Supabase" — هذا طبيعي لأنك بنيت أغلب التطبيق بلوفابل مباشرة. **Supabase هي نفسها لوحة "Cloud" الموجودة بلوفابل** (اللي فيها Database، Secrets، SQL editor، Edge functions) — مو خدمة منفصلة تحتاج حساب جديد لها. أي تعليمات جاية مني بخصوص "افتح Supabase" تقصد بالضبط هذي اللوحة اللي عندك أصلاً بلوفابل.

---

# تحديثات غالِب — الدفعة الحادية عشرة

## طريقة التطبيق
فك الضغط داخل المشروع. لا يوجد مايجريشن جديد بهذي الدفعة.

## إصلاح PDF (السبب الحقيقي، هالمرة بالضبط)

رسالة الخطأ اللي أرسلتها كانت حاسمة: `unsupported color function "oklab"`. تبيّن إن إصلاحي بالدفعة الماضية كان ناقص — كنت أحوّل الألوان عن طريق قراءة `canvas.fillStyle` بعد تعيينه، على افتراض إن المتصفح يرجعها دايمًا بصيغة `rgb()`. الافتراض غلط: بعض المتصفحات ترجعها بصيغة `oklab()` بدل `rgb()`، وhtml2canvas ما يفهم هذي ولا تلك. الحل الصحيح: تحويل اللون لبكسل فعلي بـcanvas 1×1 وقراءة البايتات الخام (`getImageData`) — هذي مضمونة تكون أرقام RGB عادية دايمًا، ما فيه أي غموض بصيغة. طبّقت هذا كمان على التدرجات اللونية (gradients) والظلال (box-shadow) ورسومات SVG، مو بس الألوان المفردة — `src/lib/export-pdf.ts`.

## مخطط المسبقات: غرافيكس أقوى

- شريط تقدّم أعلى المخطط (كم مادة أنجزت من الإجمالي ونسبتها).
- المواد "المتاحة الآن" (تقدر تسجلها) لها توهج نابض خفيف يميّزها كهدفك الجاي.
- علامة ✓ على المواد المكتملة.
- المسارات (الأسهم) المؤدية لمادة متاحة أو حالية تتحرك بشكل متدفق بدل خط ثابت.
- قائمة التصنيفات صارت أزرار فلترة تفاعلية — اضغط على تصنيف يعتّم الباقي ليسهل عليك تتبع قسم معيّن — `src/components/app/PrereqFlowChart.tsx`, `src/styles.css`.

## المرشد الذكي: ملاحظات استباقية

الحين لما تفتح صفحة المرشد الذكي (قبل لا تسأل أي شي)، يطلع لك تلقائيًا حتى ٤ ملاحظات محسوبة من بياناتك الفعلية — بدون أي طلب ذكاء اصطناعي (فورية ومجانية):
- أسبوع مزدحم (٣+ مهام خلال ٧ أيام)
- فرصة إعادة مادة (تقدير C- فأقل ما أعدتها بعد)
- قرب من التفوق (معدل بين ٣.٣ و٣.٥)
- قرب كابستون ١ (بين ٨٠-٩٦ وحدة)
- مادة عالية التأثير (تسجيلها يفتح عدة مواد ثانية دفعة وحدة)
- سترييك متوقف كان طويل
- معدل هالفصل نازل عن اللي قبله

كل ملاحظة نص عليها زر — تضغطه يرسل سؤال جاهز للمرشد مبني عليها، فتتحول الملاحظة لمحادثة مباشرة — `src/lib/insights.ts`, `src/components/app/AdvisorChat.tsx`.

---

# تحديثات غالِب — الدفعة العاشرة

## طريقة التطبيق
فك الضغط داخل المشروع. **هذي الدفعة فيها migration جديد** — لازم تطبّقه على قاعدة بياناتك (`supabase db push` أو عن طريق SQL Editor بلوحة Supabase): ملف `supabase/migrations/20260910130000_add_prep_category.sql`.

## إصلاح خلل الـPDF (السبب الحقيقي)

لقيته: كنت أستخدم `getComputedStyle()` من نافذة المتصفح الرئيسية على عناصر تعيش بمستند منسوخ (clone) بإطار/نافذة مختلفة تمامًا — يستخدمها html2canvas داخليًا. هذا استخدام غير صالح تقنيًا ويطيح الكود بصمت. صححته يستخدم نافذة المستند الصحيح، وغلّفت الجزء الحساس بـ`try/catch` احتياطي، وأضفت تسجيل بالـConsole لأي خطأ مستقبلي — `src/lib/export-pdf.ts`.

## حل مشكلة "٨ فصول" الوهمية

اكتشفت السبب: فصول فاضية (بدون أي مادة) انزرعت بجدول `terms` غالبًا من محاولة تنظيم الفصول القديمة يدويًا. الحين:
- كشف الدرجات يتجاهل أي فصل فاضي غير نشط (بالعرض والعدّاد) — `src/routes/_authenticated/transcript.tsx`.
- قسم جديد "فصول فاضية" بصفحة تصحيح تطور المعدل (بالملف الأكاديمي) يوريك أي فصل بدون مواد وتقدر تحذفه مباشرة.
- ربط موادك القديمة (بدون فصل) بأي فصل تضيفه أو تعدّله صار أوضح: قائمة اختيار صريحة تظهر بنفس نافذة الإضافة/التعديل، مو مربوطة بشرط خفي — `src/components/app/GpaHistoryManager.tsx`.

## تصنيف "تمهيدي" جديد

مادة بتصنيف "تمهيدي" (بدون كردت) — تظهر بقائمة اختيار التصنيف بكل مكان (إضافة مادة، الاستيراد)، ومستثناة من حساب خارطة التخرج لأنها مو متطلب تخرج حقيقي — `src/lib/plan.ts`, `supabase/migrations/20260910130000_add_prep_category.sql`.

## الألوان: شلت آخر ٣، وأضفت توليفة خاصة بالكامل

شلت آخر ٣ ألوان من القائمة زي ما طلبت. وبدلها أضفت قسم "توليفتك الخاصة" تحت منتقي الألوان — منتقيين لون منفصلين (خلفية + أزرار)، تختارهم بنفسك بدل التوليفات الجاهزة. تقنيًا: يحوّل لون الخلفية اللي تختاره لدرجة OKLCH (نفس نظام الألوان بالتطبيق) ويطبّقها على كل عناصر الخلفية بنفس درجة الوضوح الحالية (يتغيّر بس اللون، مو التباين) — `src/lib/theme.tsx`, `src/components/app/ThemeControls.tsx`.

## خارطة التخرج (ميزة جديدة بالكامل)

صفحة جديدة `/roadmap` (بالشريط الجانبي) تجاوب على "وش باقي علي بالضبط ومتى أتخرج":
- دائرة تقدّم شاملة (النسبة المئوية والوحدات المنجزة من الإجمالي).
- تقسيم حسب نوع المتطلب (عام/كلية/تخصص/تخصص اختياري) مع شريط تقدّم لكل قسم وقائمة المواد الباقية.
- تقدير فصل التخرج المتوقع بناءً على وتيرتك (وحدات/فصل، قابلة للتعديل) وفصلك الحالي.
- المادة الإجمالية المطلوبة = كل موادك المُدخلة بالتطبيق (بافتراض إن خطتك بالتطبيق هي خطتك الكاملة) — `src/lib/roadmap.ts`, `src/routes/_authenticated/roadmap.tsx`.

---

# تحديثات غالِب — الدفعة التاسعة

## طريقة التطبيق
فك الضغط داخل المشروع. لا يوجد مايجريشن جديد.

## لقيت سبب اختفاء الفصل الأول (تمهيدي، معدل صفر)

مواد الفصل التمهيدي (زي IEP099) اللي استوردتها بالبداية غالبًا ما انحطلها اسم فصل (`completed_term`) أثناء الاستيراد — والكود كان **يتجاهل بصمت** أي مادة مكتملة بدون اسم فصل بدل ما يعرضها، فتختفي من كشف الدرجات والإنجازات وتطور المعدل بالكامل. صححت هذا: الحين تظهر كـ"فصل بدون اسم" (مميز بلون تنبيه) بقسم "تطوّر المعدل" بالملف الأكاديمي، وتقدر تضغط تعديل وتعطيه اسم حقيقي — وبمجرد ما تحفظ، يتحدث تلقائيًا كل المواد المرتبطة فيه (مو بس اسم العرض) — `src/lib/gpa.ts`, `src/components/app/GpaHistoryManager.tsx`.

كمان أضفت **تقدير "P" (ناجح، بدون تأثير على المعدل)** كخيار درجة حقيقي بكل مكان تختار منه درجة (إنهاء الفصل، تعديل مادة، الاستيراد) — عشان مواد التمهيدي القادمة تنحفظ صح من الأول بدل "P" حرة غير معترف فيها — `src/lib/plan.ts`.

## توليفات ألوان كاملة (خلفية + أزرار مع بعض)

فهمت طلبك صح هالمرة: مو بس لون الأزرار، الخلفية نفسها تتغير حسب التوليفة. ٣ توليفات معدّلة/جديدة تغيّر الخلفية فعليًا:
- **توت العليق وليمون**: خلفية توت عليق غامقة + أزرار ليمون فاتحة.
- **ليموني وبنفسجي**: خلفية بنفسجية غامقة (Tyrian Purple) + أزرار ليمونية.
- **شوكوبايْ وزبدي** (جديدة): خلفية بنية شوكولاتة + أزرار زبدية صفراء + لون ثانوي أزرق فاتح.

تقنيًا: كل عناصر الخلفية (البطاقات، الشريط الجانبي، الحدود) تتغيّر لنفس درجة الوضوح والإشباع الحالية بالضبط — بس بلون مختلف — فالتباين والقراءة ما يتأثرون، يتغيّر بس "المزاج". النص يبقى محايد (أبيض/رمادي) بكل التوليفات لضمان الوضوح. مربع اللون بقائمة الاختيار الحين نص/نص (خلفية/أزرار) عشان توليفات الكومبو تكون واضحة قبل لا تجربها — `src/lib/theme.tsx`.

## بخصوص "تفعيل" البريفنج الصباحي، المرشد الذكي، وأدوات الدراسة

راجعت كود الثلاثة، والوضع مختلف لكل واحد:

- **المرشد الذكي وأدوات الدراسة**: الكود كامل ويعتمد على مفتاح الذكاء الاصطناعي الخاص بـLovable نفسها (`LOVABLE_API_KEY`) — إذا مشروعك مربوط بـLovable Cloud بشكل طبيعي، هذا المفتاح موجود تلقائيًا بدون أي إعداد إضافي منك. **جربهم الحين مباشرة** — إذا طلعت لك رسالة خطأ، صوّرها لي وأشوف السبب بالضبط.
- **البريفنج الصباحي**: هذا مختلف — يحتاج خطوات إعداد حقيقية بره الكود (مفاتيح VAPID، مفتاح خرائط جوجل، جدولة cron بقاعدة بياناتك) موثّقة بالتفصيل بملف `EMERGENCE.md` بالمشروع (٦ خطوات، كل وحدة أوامر جاهزة تنسخها/تلصقها). هذي خطوات لازم تسويها أنت شخصيًا لأنها تحتاج حسابك بـSupabase وGoogle Cloud — ما أقدر أنفذها من مكاني. **قلي إذا تبي نسويها مع بعض خطوة بخطوة** بالرد الجاي وأمشي وياك بكل أمر.

---

# تحديثات غالِب — الدفعة الثامنة

## طريقة التطبيق
فك الضغط داخل المشروع (استبدال الملفات). لا يوجد مايجريشن جديد.

## سبب ألوان "تطوّر المعدل" غير واضحة — وُجد الخلل

الكود كان يكتب `stroke="hsl(var(--accent))"` وأمثالها، لكن كل ألوان هذا التطبيق معرّفة بصيغة `oklch(...)` مو `hsl(...)` — يعني كان يطلب من المتصفح لون غير صالح أصلًا (`hsl(oklch(...))`)، فيتجاهله المتصفح ويطلع بلا لون واضح. صححت كل الإشارات وأعدت تصميم الرسم بالكامل ليطابق الشكل المطلوب: خطين (معدل الفصل + المعدل التراكمي) بلونين مختلفين، تسمية كل نقطة بقيمتها، ليجند، وشبكة خلفية — `src/components/app/GpaTrendChart.tsx`.

- نفس اكتشاف الخلل طبّقته على تلوين مربعات "نشاط اليوم" (كانت بلون أزرق ثابت مو لون الثيم) — الحين تتبع لون الثيم المختار فعليًا — `src/components/app/StudyStreak.tsx`.

## الثيم والتباين

- تباين نص الأزرار مع لون الثيم كان مطبّق أصلًا للألوان المخصصة (يحسب تلقائيًا أسود أو أبيض حسب سطوع اللون). أضفت **٥ توليفات ألوان جاهزة** مستوحاة من الصور اللي أرسلتها (توت العليق، ليموني/بنفسجي، ياقوتي وخوخي، مشمس، برتقالي ووردي) بنفس منطق التباين المدروس — تظهر بقائمة اختيار الثيم بالإعدادات — `src/lib/theme.tsx`.
- **ملاحظة صريحة**: ما غيّرت لون الخلفية الرئيسي (الكحلي) ليتبع كل ثيم تلقائيًا — هذا تغيير أكبر وأخطر (يمس كل عنصر بالتطبيق) ما أقدر أتحقق منه بصريًا بدون تجربة حية. توليفة "ياقوتي وخوخي" تحديدًا مصممة عشان تنسجم مع الخلفية الكحلية الافتراضية الموجودة أصلًا بالتطبيق.

## التقويم: شكلين مختلفين

- تقويم الصفحة الرئيسية افتراضيًا **أسبوعي** الحين، وتقويم صفحة "التقويم" المخصصة يبقى **شهري** — `src/components/app/CalendarView.tsx` (خاصية `defaultView` جديدة)، `src/routes/_authenticated/dashboard.tsx`.

## زر تحميل PDF: احتمالان للسبب، عالجت الاثنين

- **الاحتمال الأرجح**: مكتبة html2canvas (اللي تصوّر الصفحة) ما تفهم صيغة الألوان `oklch()` اللي يستخدمها التطبيق بالكامل — يعني يفشل بالضبط بهذا الشكل المتكرر بكل صفحة. أضفت تحويل تلقائي لكل الألوان لصيغة `rgb()` القياسية عن طريق Canvas API (بدون أي مكتبة إضافية) قبل التصوير مباشرة — يفترض يحل المشكلة نهائيًا.
- **احتمال إضافي**: لو المشكلة شبكة/حجب، أضفت مصدر بديل (jsdelivr) يجرّبه تلقائيًا لو cdnjs فشل.
- ورسالة الخطأ الحين تفرّق بين "مكتبات ما تحمّلت" و"خطأ غير متوقع" بدل رسالة عامة واحدة — `src/lib/export-pdf.ts`.
- **إذا استمرت المشكلة رغم هذا**: افتح Console بالمتصفح (F12) وقت الضغط على الزر وأرسللي لقطة شاشة لأي خطأ أحمر يطلع — بيدلني بالضبط وين المشكلة بدل التخمين.

---

# تحديثات غالِب — الدفعة السابعة

## طريقة التطبيق
فك الضغط داخل المشروع (استبدال الملفات). لا يوجد مايجريشن جديد.

## PDF حقيقي (مو طباعة شاشة)

- **زر "تحميل PDF" جديد** بصفحات كشف الدرجات، تقويم الفصل، وطباعة الخطة — بجانب زر الطباعة العادي. يلتقط محتوى الصفحة فقط (بدون أي شريط تنقل) كصورة عالية الدقة من نفس عرض المتصفح (فيحافظ على شكل العربي والاتجاه RTL صحيح ١٠٠٪) ويحوّلها لملف PDF حقيقي يتنزل مباشرة — مو مربع طباعة المتصفح.
- تقنيًا: يحمّل مكتبتين (html2canvas + jsPDF) وقت الحاجة من CDN بدل ما يضيفهم كـdependency بالمشروع — هذا لأن jsPDF ما يدعم العربي أصلاً (لا خطوط عربية ولا تشكيل حروف)، فالحل الأوثق هو تصوير المحتوى زي ما المتصفح يعرضه فعليًا (يحل مشكلة العربي تلقائيًا) بدل رسم نص عربي مباشر بالمكتبة. **يحتاج إنترنت أول مرة** يستخدمه المستخدم بالمتصفح (تحميل المكتبتين)، وبعدها متخزنة بالمتصفح. — `src/lib/export-pdf.ts`، `src/styles.css` (كلاس `.pdf-capturing`).
- ملاحظة: الملف الناتج صفحة واحدة طويلة بحجم المحتوى (مو مقسّم لصفحات A4) — يفتح ويطبع بشكل طبيعي بأي قارئ PDF.

## استيراد كشف الدرجات من صورة أو ملف

- **قسم "تطوّر المعدل" بالملف الأكاديمي صار فيه زر "استيراد من صورة أو ملف كشف الدرجات"**: ترفع صورة (لقطة شاشة/تصوير بالجوال) أو PDF لكشف درجاتك، والذكاء الاصطناعي يستخرج كل فصل فيه (الاسم، المعدل، الوحدات) — تراجع النتائج وتلغي تحديد أي فصل غلط قبل الحفظ. نفس آلية استيراد الخطة الدراسية الموجودة أصلًا بالتطبيق (نفس الـAI gateway)، بس لكشف الدرجات — `src/lib/transcript-import.functions.ts`، `src/components/app/GpaHistoryManager.tsx`.

## إصلاحات إضافية أبلغ عنها المستخدم

- **تسجيل مادة مع لابها بنفس الفصل**: أي مادة اسمها يحتوي كلمة "Lab" (بأي مكان بالاسم) تُكتشف تلقائيًا كلاب لمادتها الأم، وتصير متاحة بمجرد ما تكون المادة الأم متاحة أو حالية — بدون انتظار اكتمالها. ينطبق على مخطط المسبقات، محاكي التسجيل، و"فتح الفصل الجاي" — `src/lib/plan.ts` (`isLabPairOf`).
- **"ماذا لو — إعادة مادة"** بصفحة حاسبة الـGPA: زر يعرض موادك المكتملة بتقدير C- فأقل (ومو معاد تسجيلها مسبقًا)، تضيفها للمحاكاة وتجرب لها تقدير افتراضي — يستبدل تقديرها القديم مو يضيفه فوقه — `src/lib/gpa.ts`، `src/routes/_authenticated/gpa-planner.tsx`.
- **تسميات "تطور المعدل"**: أي فصل تسميته رقم مجرد (بيانات مستوردة قديمة) يظهر الحين "الفصل ٢" بدل رقم بمفرده.

---

# تحديثات غالِب — الدفعة السادسة

## طريقة التطبيق
فك الضغط داخل المشروع (استبدال الملفات). لا يوجد مايجريشن جديد.

## إصلاحات وميزات جديدة

- **تسجيل المادة مع لابها بنفس الفصل**: أضفت استثناء "متطلب مصاحب" — أي مادة اسمها "X Lab" (أو "Lab" بمنتصف الاسم زي "Physics Lab I") تُكتشف تلقائيًا كلاب لمادتها (بمطابقة الاسم بعد حذف كلمة Lab)، وتصير "متاحة" بمجرد ما تكون المادة الأم متاحة أو حالية — بدون ما تنتظر اكتمالها فعليًا. ينطبق على مخطط المسبقات ومحاكي التسجيل و"فتح الفصل الجاي" — `src/lib/plan.ts`.
- **زر "ماذا لو — إعادة مادة"** بصفحة حاسبة الـGPA: يعرض قائمة موادك المكتملة بتقدير C- فأقل (ومو معاد تسجيلها مسبقًا)، تختار منها وتضيفها للمحاكاة مع بقية مواد الفصل الحالي، وتجرب لها تقدير افتراضي وتشوف أثره على معدلك — الحساب يستبدل تقديرها القديم مو يضيفه فوق (زي الإعادة الحقيقية بالضبط) — `src/lib/gpa.ts`, `src/routes/_authenticated/gpa-planner.tsx`.
- **قسم جديد بالملف الأكاديمي: "تطوّر المعدل — تصحيح البيانات"**: يعرض كل فصولك المكتشفة (حتى اللي ما إلها صف حقيقي بجدول `terms`)، وتقدر تضيف/تعدّل اسم ومعدل ووحدات أي فصل يدويًا لتصحيح أي بيانات غير دقيقة — `src/components/app/GpaHistoryManager.tsx`. الاستيراد التلقائي من صورة/ملف كشف الدرجات مو متاح بعد (يحتاج بنية OCR ما بنيتها) — نبّهت على هذا بالواجهة بدل ما أدّعي إنه شغال.
- **تحسين تسميات "تطور المعدل"**: أي فصل تسميته رقم فقط (بسبب بيانات مستوردة قديمة) يظهر الحين "الفصل ٢" بدل رقم مجرد — `src/components/app/GpaTrendChart.tsx`.

## توضيح بخصوص الطباعة
أصلحت بالدفعة الماضية مشكلة طباعة الشاشة كاملة (كانت صفحتا تقويم الفصل وكشف الدرجات ناقصات من قائمة "بدون سايدبار"). تأكدت إن الإصلاح لسه موجود بالكود. لكن "حفظ كـPDF" حاليًا يعتمد على مربع طباعة المتصفح (تختار "Save as PDF" كوجهة) — مو توليد ملف PDF حقيقي من التطبيق نفسه. توليد PDF مخصص فعليًا (بدون مربع طباعة المتصفح) يحتاج مكتبة جديدة (زي jsPDF) ما قدرت أضيفها وأتأكد إنها تشتغل صح بهذي البيئة (ما عندي إنترنت لتحميل باكجات جديدة والتحقق منها). إذا تبيها ميزة منفصلة، أقدر أبنيها بجلسة قادمة.

---

# تحديثات غالِب — الدفعة الخامسة

## طريقة التطبيق
فك الضغط داخل المشروع (استبدال الملفات). لا يوجد مايجريشن جديد.

## إصلاح أخطاء أبلغ عنها المستخدم

- **الطباعة تطبع الشاشة كاملة بدل صفحة منسّقة**: صفحتا "تقويم الفصل" و"كشف الدرجات" ما كانتا مضافتين لقائمة الصفحات "بدون سايدبار" بالتخطيط، فكان يطبع السايدبار والقائمة الجانبية معهم. أضفتهم — `src/routes/_authenticated/route.tsx`.
- **التقويم افتراضيًا شهري بدل أسبوعي** — `src/components/app/CalendarView.tsx`.
- **تقويم الفصل أعيد بناؤه بالكامل** ليطابق الطلب الفعلي: كل شهر قسم مستقل، تحته صفوف للأسابيع، وكل أسبوع ٧ أعمدة للأيام (تصميمي السابق كان بالعكس — شهور كأعمدة) — `src/components/app/SemesterGrid.tsx`.
- **زر تكبير خريطة المسبقات**: تبيّن إنه كان فعليًا زر "إعادة الحجم لـ١٠٠٪" مموّه بنفس الأيقونة. أضفت زر توسيع حقيقي يفتح الخريطة بنافذة كبيرة، وفصلت زر إعادة الحجم لأيقونة ثانية — `src/components/app/PrereqFlowChart.tsx`.
- **"تطور المعدل" والإنجازات تقول ما أنهيت فصل رغم إنك بالفصل الرابع**: السبب أن الفصول القديمة المستوردة عن طريق "onboarding" ما تُنشئ صف بجدول `terms` (بس تحدّث المعدل التراكمي بالبروفايل). بنيت `deriveTermHistory()` تجمع الفصول من المواد المكتملة نفسها بدل الاعتماد على جدول ممكن يكون فاضي — `src/lib/gpa.ts`, `src/components/app/GpaTrendChart.tsx`, `src/lib/achievements.ts`.
- **سلّم الدرجات**: شلت A+ (غير موجودة بجامعتك)، صححت كل النقاط لتطابق سلّمكم بالضبط، وأضفت FA — تغيير مركزي بـ`GRADE_SCALE` ينعكس بكل مكان يختار منه درجة — `src/lib/plan.ts`.
- **كابستون ١ يظهر رغم عدم اكتمال ٩٦ وحدة**: الشرط كان يتوقع "Capstone" ملاصقة لـ"1" مباشرة، لكن اسم مادتك "Capstone Design 1" فيها كلمة بينهم. صححته ليتحقق فقط من احتواء الاسم على "Capstone"/"كابستون" وانتهائه بـ"1"/"١" — `src/lib/plan.ts`. هذا يفسر أيضًا لماذا "اقترح أفضل تشكيلة" ما كان يتغيّر (كانت تدخل بثبات ضمن كل نتيجة).

## ميزة جديدة

- **زر "إعادة المادة"**: يظهر بصفحة أي مادة مكتملة بتقدير C- أو أقل (وبس)، وعند الضغط ينشئ نسخة جديدة من نفس المادة كمادة "مستقبلية" مربوطة بالمحاولة السابقة (`is_retake` + `previous_attempt_id`، نفس آلية الإعادة الموجودة أصلًا بنموذج إضافة مادة) — `src/routes/_authenticated/courses.$courseId.tsx`.

---

# تحديثات غالِب — الدفعة الرابعة

## طريقة التطبيق
فك الضغط داخل المشروع (استبدال الملفات). لا يوجد مايجريشن جديد — كل شي client-side.

## الإضافات

- **كشف الدرجات (Transcript)**: صفحة `/transcript` جديدة — مجمّعة حسب السنة الدراسية ثم الفصل، بنفس بيانات كشفك (رمز/اسم المادة، الوحدات، الدرجة، إحصائيات الفصل والتراكمي)، بس بتصميم بطاقات وألوان حسب مستوى الدرجة بدل جدول نصي، وزر طباعة/حفظ PDF. الوصول لها من زر بصفحة "الملف الأكاديمي" — `src/routes/_authenticated/transcript.tsx`.
- **إعادة تصميم "الملف الأكاديمي"**: هيدر بطاقة متدرّجة، شريط إحصائيات بأيقونات، وكل قسم صار بعنوان+أيقونة+خلفية موحّدة (`SectionCard`) بدل نص عادي — `src/routes/_authenticated/profile.tsx`.
- **إعادة تصميم "المرشد الذكي"**: هيدر بطاقة متدرّجة، أفاتار للمساعد على كل رسالة، اقتراحات بأيقونات، تمييز أوضح للمحادثة النشطة بالقائمة الجانبية — `src/routes/_authenticated/advisor.tsx`, `src/components/app/AdvisorChat.tsx`.
- **الشارات والأصوات**: ٨ شارات إنجاز تُحسب تلقائيًا من بياناتك الموجودة (أول مادة، أول فصل، ٣٠/٦٠/٩٦ وحدة، معدل ٣.٥+، ستريك ٧/٣٠ يوم) — بدون أي عمود جديد بقاعدة البيانات. عند فتح شارة جديدة يطلع toast + نغمة قصيرة (Web Audio، بدون ملف صوتي خارجي)، مع زر لكتم الصوت. تظهر بقسم جديد بصفحة الملف الأكاديمي — `src/lib/achievements.ts`, `src/lib/sound.ts`, `src/components/app/AchievementsBadges.tsx`.

---

# تحديثات غالِب — الدفعة الثالثة

## طريقة التطبيق
1. فك ضغط هذا الملف داخل مجلد المشروع (استبدال الملفات الموجودة بنفس الأسماء) — لا يوجد مايجريشن جديد في هذه الدفعة.
2. إذا تبي التنبيهات وبحث المواقع تشتغل، عبّي `VITE_VAPID_PUBLIC_KEY` و`VITE_GOOGLE_MAPS_API_KEY` في `.env` (شرح كامل بـ EMERGENCE.md).
3. `git add . && git commit -m "feat: registration simulator variety, capstone unit gate, term calendar cleanup, goal tool reset" && git push`

## إصلاحات وتعديلات

- **زر "اختر أفضل تشكيلة" (محاكي التسجيل)**: كان يعطي نفس التشكيلة في كل مرة. الحين كل ضغطة تعطي تشكيلة مختلفة (بنفس الجودة/عدد الفتح) بدل ما يكرر نفس الاقتراح — `src/lib/plan.ts`, `src/routes/_authenticated/simulator.tsx`.
- **Capstone 1**: ما يظهر كمادة "متاحة" (سواء بمخطط المسبقات أو محاكي التسجيل أو "فتح الفصل الجاي") إلا بعد اجتياز ٩٦ وحدة فأكثر، بالإضافة لأي متطلبات سابقة عادية — `src/lib/plan.ts` (دالة `isCapstoneOne` تتعرف عليها بالاسم/الرمز، ما تحتاج عمود جديد بقاعدة البيانات).
- **صفحة "الوصول لهدف معين" (GPA Planner)**: كانت تحفظ آخر قيمة كتبتها بالـ localStorage. الحين ترجع لوضعها الافتراضي فارغة كل ما تدخلها من صفحة ثانية — `src/routes/_authenticated/gpa-planner.tsx`.
- **مخطط المسبقات (Prerequisites Flow Chart)**: الحجم الافتراضي صار ١٠٠٪ دايمًا (بدل التصغير التلقائي للمخططات الكبيرة)، ولو غيّرت الحجم يدويًا ينحفظ ويرجع لك بنفس القيمة المرة الجاية — لين تضغط "إعادة تعيين" — `src/components/app/PrereqFlowChart.tsx`.
- **تقويم الفصل (Term Calendar)**: انشال من الشريط الجانبي، وصار الوصول له عن طريق زر داخل صفحة "التقويم" وزر داخل "تعديل الفصل" بالرئيسية — `src/components/app/AppSidebar.tsx`, `src/routes/_authenticated/calendar.tsx`.
- **تطور المعدل (GPA Trend)**: إذا عندك أكثر من فصلين ولكن معدلات بعضها غير مسجلة، يطلب منك إضافتها بدل رسالة عامة — `src/components/app/GpaTrendChart.tsx`.
- **إنهاء الفصل بمعدل ٠.٠**: لو معدل الفصل طلع بالضبط ٠.٠، يسألك قبل الحفظ إذا هو فصل تمهيدي (بدون كردت، ما يدخل بحساب المعدل التراكمي) أو فصل عادي عليه كردت — `src/components/app/TermControls.tsx`.

## لسه ناقص (مو بهذه الدفعة)
كشف الدرجات القابل للتصدير (PDF)، إعادة تصميم صفحتي الملف الأكاديمي والمرشد الذكي، الشارات والأصوات على الإنجازات — تحتاج جلسة عمل منفصلة (تصميم/تكرار بصري).

---

# تحديثات غالِب — الدفعة الثانية

## طريقة التطبيق
1. فك ضغط هذا الملف داخل مجلد المشروع (استبدال الملفات الموجودة بنفس الأسماء).
2. طبّق المايجريشنين الجديدين:
   - `supabase/migrations/20260830100000_push_and_briefing.sql`
   - `supabase/migrations/20260830110000_study_streak.sql`
3. اقرأ **EMERGENCE.md** بالتفصيل — فيه كل خطوة نشر ناقصة (VAPID keys، Google Maps API key، جدولة pg_cron) بترتيب تنفيذي واضح.
4. `git add . && git commit -m "feat: push notifications, morning briefing, study streak; fix what-if GPA scope" && git push`

## إصلاح
- **حاسبة What-if للـGPA**: صارت تعرض فقط مواد الفصل الحالي (`status === "current"`) بدل عرض كل المواد المستقبلية أيضاً.

## مميزات جديدة (كود كامل، النشر الفعلي يحتاج خطوات من EMERGENCE.md)

### 1. تنبيهات Push حقيقية
- `public/sw.js` — Service Worker
- `src/lib/push.ts` + `src/hooks/usePushNotifications.ts` — اشتراك/إلغاء اشتراك العميل
- `supabase/functions/_shared/{time,push}.ts` — منطق زمني ومنطق إرسال مشترك
- `supabase/functions/send-reminders/index.ts` — Edge Function تُرسل تذكيرات المهام/الأحداث الفعلية

### 2. البريفنج الصباحي
- `supabase/functions/morning-briefing/index.ts` — يحسب وقت الخروج المثالي (حركة مرور + طقس + عدد محاضرات + اختبارات اليوم) ويرسله قبل أول محاضرة بساعة
- واجهة إعدادات كاملة في `src/routes/_authenticated/profile.tsx` (موقع البيت عبر GPS، موقع الجامعة يدوي، وسيلة تنقل، هامش الأمان)

### 3. ستريك الحضور/الدراسة (heatmap على طراز GitHub)
- `src/lib/streak.ts` — منطق بناء الشبكة وحساب الستريك (تحقق منه بـ4 مناطق زمنية مختلفة)
- `src/components/app/StudyStreak.tsx` — المكوّن البصري، مدمج بالداشبورد

## ملفات معدّلة أخرى
- `src/integrations/supabase/types.ts` — جداول/أعمدة جديدة: `push_subscriptions`, `briefing_log`, `study_streak`, + أعمدة `profiles`/`calendar_events`/`course_items`
- `src/lib/queries.ts` — `streakQuery`, `logStreakToday`
- `src/lib/i18n.tsx` — ~35 مفتاح ترجمة جديد
- `src/routes/_authenticated/dashboard.tsx` — إدراج `<StudyStreak />`

## ملاحظة تقنية مهمة
كل حسابات الوقت (التذكيرات + البريفنج) تمر عبر `profiles.timezone` (افتراضي `Asia/Riyadh`) - **لازم** تتأكد إن هذا صحيح لمستخدميك، وإلا التنبيهات بتوصل بوقت غلط.
