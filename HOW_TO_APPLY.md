# حزمة كاملة — إصلاحات جولة "قبل الاستخدام الفعلي"

## طريقة التطبيق (استبدال كامل)
1. احذف مجلدات `src`، `supabase`، `public` بالكامل من الريبو المحلي عندك.
2. انسخ نفس المجلدات من هذا الأرشيف مكانهم.
3. استبدل `vite.config.ts` و`package.json` بالنسختين هنا.
4. **مهم جداً**: طبّق كل الـmigrations الجديدة (بالترتيب):
   - `20260830100000_push_and_briefing.sql`
   - `20260830110000_study_streak.sql`
   - `20260831090000_ai_usage_log.sql`
   - `20260831120000_chat_sessions.sql`
5. أضف لملف `.env` المحلي عندك (إذا ما أضفتها بعد):
   - `LOVABLE_API_KEY` — بدونه، المرشد وأدوات الدراسة يفشلون بالكامل
   - `VITE_GOOGLE_MAPS_API_KEY` — بدونه، بحث موقع الجامعة ما يشتغل (يبان تنبيه واضح بدلاً من فشل صامت)
6. `npm run dev`

## أهم إصلاح تشخيصي بهذي الجولة
لو شفت "تعذّر الحفظ" أو "تعذّر الرد" بعد تطبيق كل الخطوات فوق، الرسالة نفسها الحين بتوضح لك السبب
بدل ما تكون مبهمة — إما "طبّق آخر migration" أو "مفتاح AI ناقص" أو "مفتاح خرائط ناقص"، مو رسالة
عامة بعد الآن.

## الإصلاحات والمميزات بهذي الجولة
- التايم زون الافتراضي: الكويت (بدل الرياض) بكل مكان
- خريطة المتطلبات: zoom/pan حقيقي (أزرار +/- وسحب بالماوس)
- المرشد الأكاديمي: إعادة تصميم كامل — سايد بار بكل المحادثات السابقة، زر "محادثة جديدة"، كل صفحة
  جديدة تبدأ محادثة فاضية (يحل تراكم السجل + طلبك بشكل ChatGPT)
- إصلاح تنبيهات مزدوجة: تنبيه المتصفح المباشر يوقف نفسه تلقائياً لو الجهاز مفعّل عليه Push حقيقي
- أدوات الدراسة والمرشد: رسائل خطأ محددة الآن (مفتاح AI ناقص / حد الطلبات / فشل حفظ) بدل رسائل مبهمة
- حاسبة What-if: قسم الهدف المعيّن يحفظ قيمك بالمتصفح، ما يرجع للقيمة الافتراضية عند تغيير الصفحة
- بدء فصل جديد: حقل بحث بقائمة اختيار المواد
- موقع الجامعة: بحث حقيقي من خرائط جوجل بدل إدخال خطوط الطول/العرض يدوياً
- تنبيهات Push: رسالة خطأ محددة لكل سبب فشل (مفتاح VAPID ناقص / رفض الإذن / غير مدعوم)
- الشريط الجانبي: زر طي/توسيع لكل قسم (أدرسها/مكتملة/خطة مستقبلية) — "خطة مستقبلية" مطوية افتراضياً
- الستريك: كشف تلقائي بالموقع الجغرافي أثناء وقت المحاضرة (بالإضافة للتسجيل اليدوي، مو بدلاً عنه)

## ملاحظة عن "توحيد المرشد وأدوات الدراسة"
تعمّدت **عدم دمجهم** رغم إنه كان أحد الخيارين المطروحين — لأنهم يخدمون غرضين مختلفين فعلياً:
المرشد محادثة مستمرة بسياق كامل عن مواعيدك ودرجاتك، وأدوات الدراسة عمليات دفعة-وحدة على مستند
محدد (ملخص/كويز/بطاقات). دمجهم كان بيعقّد الاثنين بدون فايدة واضحة. لو لسه تحس إنهم متشابهين بعد
هالإصلاحات، خبرني ونناقشها.
